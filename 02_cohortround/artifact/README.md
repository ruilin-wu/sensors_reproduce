# Self-contained CohortRound artifact

This snapshot contains every compiler-to-Vivado file introduced by the
CohortRound pre-Vivado implementation. Start with
[`../COHORTROUND_VALIDATION_EN.md`](../COHORTROUND_VALIDATION_EN.md) for the
results, limitations, layout, and reproduction commands.

The snapshot is a copy, not a symlink. The original `flexbe-main` implementation
remains intact so existing project workflows continue to work.
